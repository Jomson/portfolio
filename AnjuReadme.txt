open git bash
navigate to a folder of your interest using cd
git clone <url of repo>
cd <press tab and select your folder of repo> 
git init
make local changes
git add .
git status
git commit -a -m <"your commit message"> 
git push
